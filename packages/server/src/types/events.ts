// Socket.IO typed events — re-exported from @dicewars/shared with lobby extensions

export type {
  ServerToClientEvents,
  ClientToServerEvents,
  InterServerEvents,
  SocketData,
} from '@dicewars/shared';
